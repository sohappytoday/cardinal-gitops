apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml
secretGenerator:
  - name: mysql-secret
    envs:
      - .env
generatorOptions:
  disableNameSuffixHash: true
